name: ReverseDNS
before: |
  <p>
    For IPs that are included by the ips setting, do a reverse lookup and place everything before the first dot in the field specified.
    <br>
    Create a <code>[reversedns]</code> section to configure
  </p>

settings:
  - key: field
    value: REQUIRED
    text: The field to set with the hostname

  - key: ips
    value: REQUIRED
    text: Semicolon separated list of IPs or CIDRs to lookups. IPs that don't match this list will NOT be reverse looked up.

  - key: servers
    value: EMPTY
    text: If set, the reversedns source will use the semicolon separated list of IP addresses for reverse lookups.

  - key: stripDomains
    value: EMPTY
    text: If EMPTY then all domains are stripped after the FIRST period. When set ONLY domains that match the semicolon separated list of domain names are modified, and only the matching part is removed. Those that don't match will be saved in full. The list is checked in order. A leading dot is recommended. For example <code>stripDomains=.foo.example.com;.example.com</code> will convert <code>test1.foo.example.com</code> to <code>test1</code>, <code>test2.bar.example.com</code> to <code>test2.bar</code> and finally <code>test3.bar.com</code> to <code>test3.bar.com</code>

  - key: cacheSize
    value: 200000
    text: Maximum number of results to cache
